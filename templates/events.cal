BEGIN:VCALENDAR
VERSION:2.0
PRODID:-//hacksw/handcal//NONSGML v1.0//EN
{%- for year in site.get('/kalender').children %}
{%- for event in year.children %}
BEGIN:VEVENT
UID:{{ event._slug }}
DTSTAMP:{{ event.date.strftime("%Y%m%dT%H%M%SZ") }}
ORGANIZER;CN=Section77:MAILTO:info@section77.de
DTSTART:{{ event.date.strftime("%Y%m%dT%H%M%SZ") }}
DTEND:{{ event.date.strftime("%Y%m%dT%H%M%SZ") }}
SUMMARY:{{ event.title }}
END:VEVENT
{%- endfor %}
{%- endfor %}
END:VCALENDAR